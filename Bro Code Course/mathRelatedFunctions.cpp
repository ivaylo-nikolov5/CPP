#include <iostream>
#include <cmath>

int main() {
    double x = 3;
    double y = 4;
    double z;

    // z = std::max(x, y);
    // z = std::min(x, y);
    // z = pow(x, y);
    // z = sqrt(9);
    // z = abs(-10);
    // z = round(3.14);
    // z = ceil(3.14);
    z = floor(3.14);

    std::cout << z;
    return 0;
}